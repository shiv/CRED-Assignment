"use strict";

var airlines = require("./airlines");
var classTypes = require("./class_types");
var locations = require("./locations");

module.exports = {
    Airline : airlines,
    ClassType : classTypes,
    Location : locations
}
